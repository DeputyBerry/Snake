// Complete the solution so that it reverses all of the words within the string passed in.

// function reverseWords(str){
//     return str.split(' ').reverse().join(' ')
//   }

// method 1: returns the string as an array of words 'split' on the space between words
// method 2: returns the arr of words in reversed order
// method 3: returns that arr as a newly joined str of words with that same space between them

// function capitalizeWord(word) {
//     return word.charAt(0).toUpperCase() + word.slice(1)
//   }

// method 1: returns the first character at the '0' position in string
// method 2: changes that character to upperCase
// method 3: ensures that only first letter of string is capitilized

// function booleanToString(b){
//     if (b == true){
//       return b.toString('true')
//     } else if (b == false){
//       return b.toString('false')
//     }
//   }

// If the parameter is true, convert that boolean to the string,'true' other wise return as string 'false'